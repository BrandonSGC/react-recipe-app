import { Layout, RegisterForm } from "../../components/auth";

const Register = () => {
  return (
    <>
      <Layout>
        <RegisterForm />
      </Layout>
    </>
  );
};

export default Register;
